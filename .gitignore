# Secrets
.env
.env.local
*.env

# Python
__pycache__/
*.pyc
.venv/
.pytest_cache/

# Data & index artifacts (regenerate via scripts)
backend/data/
# chroma_db is intentionally committed (1.5 MB) so Railway can COPY it into the Docker image

# Node / Next.js
node_modules/
.next/
out/
next-env.d.ts

# OS / editor
.DS_Store
*.log
